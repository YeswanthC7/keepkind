spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/keepkind
    username: keepkind
    password: keepkind
  sql:
    init:
      mode: never
server:
  port: 8080

keepkind:
  ollama:
    baseUrl: http://localhost:11434
    chatModel: llama3
    embedModel: nomic-embed-text
